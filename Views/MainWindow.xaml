<Window x:Class="AdGuardHomeGUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AdGuard Home GUI"
        Width="420"
        Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Icon="/Assets/AdGuard.ico">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="AdGuard Home"
            FontSize="22"
            FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Margin="0,20,0,20">

            <StackPanel Orientation="Horizontal">

                <Ellipse
                    x:Name="ServiceIndicator"
                    Width="12"
                    Height="12"
                    Margin="0,4,8,0"
                    Fill="Gray"/>

                <TextBlock
                    x:Name="ServiceStatus"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Text="Service: Unknown"/>

            </StackPanel>

            <TextBlock
                x:Name="ProtectionStatus"
                Margin="20,10,0,0"
                FontSize="16"
                Text="Protection: Unknown"/>

        </StackPanel>

        <Grid Grid.Row="2">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button
                x:Name="StartServiceButton"
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,6,6"
                Height="42"
                Content="Start Service"
                Click="StartService_Click"/>

            <Button
                x:Name="StopServiceButton"
                Grid.Row="0"
                Grid.Column="1"
                Margin="6,0,0,6"
                Height="42"
                Content="Stop Service"
                Click="StopService_Click"/>

            <Button
                x:Name="EnableProtectionButton"
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,6,6,0"
                Height="42"
                Content="Enable"
                 Click="EnableProtection_Click"/>

            <Button
                x:Name="DisableProtectionButton"
                Grid.Row="1"
                Grid.Column="1"
                Margin="6,6,0,0"
                Height="42"
                Content="Disable"
                Click="DisableProtection_Click"/>

            <Button
                Grid.Row="2"
                Grid.ColumnSpan="2"
                Margin="0,16,0,0"
                Height="42"
                Content="Open Dashboard"
                Click="OpenDashboard_Click"/>
            
                <Button
                    Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Margin="0,16,0,0"
                    Height="42"
                    Content="Logout"
                    Click="Logout_Click"/>

        </Grid>

    </Grid>

</Window>